*.iml
.gradle/
.idea/
local.properties
build/
captures/
.externalNativeBuild/
.cxx/
app/build/

# Release signing material — NEVER commit this. Losing it means the app can never be updated
# on Play Store again under the same listing. Generated once locally and handed to the
# developer directly; keystore.properties holds the passwords the release build reads.
*.jks
release.keystore
keystore.properties
