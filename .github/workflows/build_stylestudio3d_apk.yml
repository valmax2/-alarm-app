name: Build Style Studio 3D APK

on:
  push:
    branches: [ main, master ]
    paths:
      - 'StyleStudio3D/**'
      - '.github/workflows/build_stylestudio3d_apk.yml'
  workflow_dispatch: # Permette di avviare la build manualmente da GitHub

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: StyleStudio3D

    steps:
      # 1. Scarica il codice sorgente
      - uses: actions/checkout@v4

      # 2. Configura Java 17 (richiesto da AGP 8.7.3 / compileSdk 35)
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # 3. Configura Android SDK e accetta le licenze
      - uses: android-actions/setup-android@v3

      # 4. Rende eseguibile il wrapper Gradle
      - run: chmod +x gradlew

      # 5. Compila l'APK in modalita' debug
      - run: ./gradlew assembleDebug --stacktrace

      # 6. Esegue i test unitari
      - run: ./gradlew testDebugUnitTest --stacktrace

      # 7. Carica l'APK tra gli allegati scaricabili della build
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: stylestudio3d-debug-apk
          path: StyleStudio3D/app/build/outputs/apk/debug/app-debug.apk
